func void holler(char c) {
	print c $n;
	
	mut n := 0;
	
	while (n < c - [##0 as int]) {
		print "Yoo hoo!!!!" $n;
		n := n + 1;
	}
	return;
}

main {
	call holler( ##4 );
	
	mut m := 0.34;
	call holler( ##2 );
	
	print "all done." $n;
}
