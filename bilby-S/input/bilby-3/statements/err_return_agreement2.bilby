func int syrup (int number) {
	if(number < 0) {
		return;
	}
	else {
		return number / 2;
	}
}



main {
	imm min := 90;
	imm max := 100;
	
	mut count := min;
	while (count < max) {
		count := count + 1;
	}
	print "done." $n;
}
