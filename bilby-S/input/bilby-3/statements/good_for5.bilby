main {
	for(k in <1..3>) {
		for(i in <1..5>) {
			for(j in <1..10>) {
				if(i*j*k < 10) {
					print $s$s;
				}
				else {
					if (i*j*k < 100) {
						print $s;
					}
				}
				print i*j*k $s;
			}
			print $n;
		}
		print $n;
	}
}