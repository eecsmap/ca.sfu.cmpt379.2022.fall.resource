func void syrup (int number) {
	if((number / 2) == 0) {
		return number / 2;
	} 
	else {
		return 3 * number + 1;
	}
}


main {
	imm min := 90;
	imm max := 100;
	
	mut count := min;
	while (count < max) {
		count := count + 1;
	}
	print "done." $n;
}
