main {
	imm min := 11;
	imm max := 17;
	
	mut count := min;
	while (count < max) {
		if(count == 15) {
			count := count + 1;
			continue;
		}
		print count $s;
		count := count + 1;
	}
	print "done." $n;
}
