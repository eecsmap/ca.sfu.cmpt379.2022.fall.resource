main {
	imm barn := alloc[float](5);
	mut i := 0;
	while(i < length barn) {
		barn[i] := [(3*i*i + i + 1) as float];
		print barn[i] $n;
		i := i + 1;
	}
	print $n;
	print barn[2*2 - 2] $n;
}